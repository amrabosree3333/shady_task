/// <reference types="node" />
import { BinaryLike } from 'crypto';
import { BigInteger } from 'jsbn';
export declare type hexString = string;
export interface PublicRSAKey {
    n: BigInteger;
    e: number;
}
export interface PrivateRSAKey extends PublicRSAKey {
    p: BigInteger;
    q: BigInteger;
    d: BigInteger;
    dmp1: BigInteger;
    dmq1: BigInteger;
    coeff: BigInteger;
}
export declare function sign(key: PrivateRSAKey, blindedMessageHash: hexString): hexString;
export declare function verify(key: PublicRSAKey, message: BinaryLike, signature: hexString): boolean;

"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const crypto_1 = require("crypto");
const HASH_FUNC = 'sha256';
const HASH_BYTES = 32;
function longHash(bits, data) {
    const bytes = Math.ceil(bits / 8);
    const output = Buffer.alloc(bytes);
    for (let nonce = 0; nonce < Math.ceil(bytes / HASH_BYTES); ++nonce) {
        const hashSegment = crypto_1.createHmac(HASH_FUNC, nonce.toString())
            .update(data)
            .digest();
        hashSegment.copy(output, nonce * HASH_BYTES);
    }
    return output;
}
exports.longHash = longHash;
